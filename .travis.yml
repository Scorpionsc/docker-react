sudo: required
services:
  - docker

before_install:
  - docker build -t scorpionsc/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true scorpionsc/docker-react npm run test -- --coverage
